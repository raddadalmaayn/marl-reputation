name: config10_provenance_replay
description: "Attack 8: Provenance replay — should be 100% blocked"
n_agents: 20
max_steps: 100
episodes: 5000
eval_episodes: 20
seeds: 5
adversarial_ids: [0, 1, 2]
adversarial_reward_bonus: 0.8
collusion_groups: []
max_sybils_per_agent: 0
enabled_attacks: [11]
checkpoint_every: 250
convergence_reward_variance_threshold: 0.05
convergence_window: 100
max_episodes_extended: 10000
