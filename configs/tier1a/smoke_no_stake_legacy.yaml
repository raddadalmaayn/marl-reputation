name: smoke_no_stake_legacy
description: "Tier1A smoke — no_stake (min_stake=1), stake_obs_mode=legacy_normalized"
adversarial_ids: [0, 1, 2, 3, 4]
adversarial_reward_bonus: 0.5
min_stake_required: 1.0
stake_obs_mode: legacy_normalized
n_agents: 20
max_steps: 100
episodes: 150
eval_episodes: 20
seeds: 2
checkpoint_every: 1000
convergence_window: 100
convergence_reward_variance_threshold: 0.05
max_episodes_extended: 150
