name: smoke_config2_absolute
description: "Tier1A smoke — config2_mixed, stake_obs_mode=absolute_log (train.py path)"
adversarial_ids: [0, 1, 2, 3, 4]
adversarial_reward_bonus: 0.5
stake_obs_mode: absolute_log
n_agents: 20
max_steps: 100
episodes: 150
eval_episodes: 20
seeds: 2
checkpoint_every: 1000
convergence_window: 100
convergence_reward_variance_threshold: 0.05
max_episodes_extended: 150
