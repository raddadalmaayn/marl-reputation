% ===========================================================================
% V. Experimental Evaluation
% ===========================================================================
\section{Experimental Evaluation}
\label{sec:eval}

\subsection{Training Summary}

Table~\ref{tab:summary} presents the main evaluation results for all 11
configurations.  Metrics are reported as mean $\pm$ standard deviation over
the final 20\% of training episodes, averaged across 5 seeds (all seeds
completed; extended training ran up to 30{,}000 episodes for seeds that had
not converged within the initial 10{,}000-episode budget).

\input{tables/tab_summary}

The key observation is that honest behavior — measured by the fraction of
rating actions that are honest — converges to $\geq\!99.7\%$ in 8 of the 9
single-attack configurations.  The sole exception is the self-rating
configuration (C6), which shows $93.2\% \pm 13.3\%$ honest behavior across
seeds; the high variance indicates that while most seeds converge to near-100\%
honest behavior, at least one seed found a local equilibrium with sustained
self-rating attempts despite the $-5.0$ penalty.

The mixed-adversary configuration (C2) shows a mean honest rate of $73.6\%$
due to one seed (seed 2) converging to an adversarial-dominant equilibrium
($0\%$ honest) after extended training.  The median across 5 seeds is
$99.7\%$, and 4 of 5 seeds converge to the honest Nash equilibrium; seed 2
represents a low-probability but non-negligible adversarial-dominant basin
reachable in the mixed-population landscape.  Both statistics are reported
to give a complete picture: the median ($99.7\%$) reflects typical behavior,
while the mean ($73.6\%$) captures the tail risk.

\subsection{Baseline Convergence (C1)}

The all-honest baseline (C1) converges to $99.8\% \pm 0.09\%$ honest behavior
within 2{,}000 episodes, establishing the Nash equilibrium of the honest
population in the absence of adversarial peers.  The mean reward of $-0.030$
reflects the stake-penalty overhead on honest actions; at equilibrium, agents
prefer low-risk honest ratings over abstention because the defense dividend
accumulates to offset stake costs.  Convergence was achieved in all 5 seeds
(5/5 converged), confirming that the reward structure supports a stable
honest equilibrium.

\subsection{Defense Performance by Attack Vector}

\subsubsection{Deterministic Defenses (C6, C7, C9)}

Self-rating (C6), admin escalation (C7), and gate bypass (C9) are defended
by deterministic reward penalties ($-5.0$, $-5.0$, $-3.0$ respectively).
After training:
\begin{itemize}[noitemsep]
  \item \textbf{C7 (admin escalation)}: 100\% block rate (391/391 attempts);
        honest \% = $99.87\% \pm 0.12\%$.  The $-5.0$ penalty dominates the
        $+1.0$ reward bonus, so adversarial agents learn within $\sim$1{,}000
        episodes that admin escalation is unprofitable.
  \item \textbf{C9 (gate bypass)}: 100\% block rate (582/582 attempts);
        honest \% = $99.74\% \pm 0.23\%$.  The $-3.0$ penalty with reward
        bonus $b=1.0$ yields an expected return of $-2.0$ per attempt;
        agents switch to honest rating actions instead.
  \item \textbf{C6 (self-rating)}: 100\% block rate (5{,}295/5{,}295 attempts);
        honest \% = $93.17\% \pm 13.28\%$.  Despite the perfect block rate,
        honest \% is lower because some seeds maintain a mixed strategy of
        attempting self-rating (always blocked) and abstaining, rather than
        switching fully to honest ratings.  The $-5.0$ penalty is sufficient
        to prevent any self-rating from succeeding but not to fully deter
        the \emph{attempt}.
\end{itemize}

\subsubsection{Probabilistic Defense (C8)}

Evidence tampering (C8) is defended with detection probability $p = 0.80$,
yielding reward $-2.0$ on detection and $+b = +1.0$ on evasion.  The
expected return per tamper attempt is:
\begin{equation}
  \mathbb{E}[R_{\text{tamper}}] = 0.80 \times (-2.0) + 0.20 \times (+1.0) = -1.4.
  \label{eq:tamper_ev}
\end{equation}
The empirical block rate of $81.5\%$ (411/507 attempts) closely matches the
designed $p=0.80$, confirming that agents learn to exploit the $20\%$
evasion probability.  Honest \% reaches $99.69\% \pm 0.13\%$, meaning that
adversarial agents attempt evidence tampering at a low rate — consistent
with the negative expected return — but do not eliminate it entirely given
the nonzero upside.

\subsubsection{Economic Deterrence (C3, C4, C5, C2)}

Sybil attacks (C3), collusion (C4), adaptive adversary (C5), and mixed
population (C2) are defended by stake slashing, CI widening, and
meta-reputation weighting rather than deterministic penalties.

\begin{itemize}[noitemsep]
  \item \textbf{C4 (collusion)}: $58.3\%$ block rate (25{,}584/43{,}799);
        honest \% = $99.46\% \pm 0.17\%$.  Collusion is partially effective:
        colluding agents can inflate scores within the group, but CI widening
        limits the magnitude of inflation visible to outside observers.
  \item \textbf{C3 (Sybil)}: $52.0\%$ block rate (24{,}361/47{,}038);
        honest \% = $87.6\% \pm 5.7\%$.  The lower honest \% compared to
        other configs reflects that Sybil creation (A5) reduces per-agent
        resources, leading some agents to attempt Sybil strategies and others
        to defect to honest rating.  The $52\%$ block rate indicates that
        approximately half of Sybil actions are blocked by the stake
        cost barrier.
  \item \textbf{C5 (adaptive, $2\times$ bonus)}: $50.1\%$ block rate
        (52{,}526/105{,}002); honest \% = $100.00\% \pm 0.01\%$ (3/5 seeds
        converged by 26{,}158 episodes; remaining seeds reached 100\% honest
        behavior without meeting the variance threshold).  Even at double
        the reward bonus, the honest population reaches a near-perfect honest
        equilibrium; the defense system handles $2\times$ adversarial incentive
        without catastrophic failure.
  \item \textbf{C2 (mixed)}: $51.9\%$ block rate (39{,}951/76{,}938);
        honest \% = $99.70\% \pm 0.22\%$.  The mixed population scenario
        without explicit attack-class actions shows the same economic
        deterrence pattern as C3--C5.
\end{itemize}

\subsubsection{Provenance Replay (C10)}

Provenance replay (C10) shows a $27.1\%$ block rate (21{,}797/80{,}451 attempts)
with honest \% = $99.86\% \pm 0.14\%$.  The low block rate arises because
the replay detection mechanism checks transaction IDs against the ledger, and
in the simulated environment, re-using IDs is structurally possible but only
detected when the ID appears in the ledger state.  This suggests that the
replay defense requires a more complete ledger-state representation in the
observation to be fully effective.  Nonetheless, the honest population
converges normally because replay attempts carry the $-5.0$ penalty regardless
of detection — they are always penalized, so adversarial agents learn to
avoid them even though the block rate metric only counts
\emph{detection-and-rejection} events.

\subsection{Comprehensive Multi-Vector Attack (C11)}

The comprehensive configuration (C11) enables all 9 attack vectors
simultaneously with 10 adversarial agents ($50\%$ of the population) in a
colluding subgroup (4 members) with Sybil capability.  Extended training
(up to 30{,}000 episodes) achieved full convergence across all 5 seeds, with
results as follows:
\begin{itemize}[noitemsep]
  \item Honest \% = $20.00\% \pm 40.00\%$ (highest variance of any configuration)
  \item Defense block rate = $48.0\%$ (72{,}507/164{,}345 attempts)
  \item Mean reward = $-2.62 \pm 2.17$ (most negative; high attack costs)
  \item Convergence episodes: 5{,}000--16{,}593 (seeds 0, 3, 4 required extended training)
\end{itemize}
The fully converged results reveal a \emph{strongly bimodal equilibrium}: 4 of 5
seeds converged to an adversarial-dominant state ($0\%$ honest, negative reward),
while 1 seed converged to the honest-dominant Nash equilibrium ($100\%$ honest).
This bimodality arises from the landscape of multi-vector coordination: once a
critical mass of adversarial agents aligns on a joint strategy early in training,
honest agents cannot recover within the shared-policy gradient.  The $48\%$ block
rate confirms that the defense layer partially degrades coordinated multi-vector
attacks, but is insufficient to guarantee honest convergence at $50\%$ adversarial
population fraction.  C11 therefore defines the practical robustness boundary of
the proposed system.

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_honest_convergence.png}
  \caption{Honest-action percentage over training episodes for all 11
           configurations (smoothed with 50-episode rolling mean).
           Single-attack configs (C6--C10) converge to $>99\%$ within
           5{,}000 episodes.  The comprehensive config (C11) shows high
           variance and partial convergence.}
  \label{fig:honest_convergence}
\end{figure}

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_reputation_accuracy.png}
  \caption{Reputation accuracy (mean $|$score $-$ true quality$|$) over
           training.  All configurations converge to $\approx\!0.30$--$0.31$
           MAE, indicating that the reputation scoring system maintains
           consistent calibration even under adversarial conditions.}
  \label{fig:rep_accuracy}
\end{figure}

\subsection{Attack Comparison}

Table~\ref{tab:attack_comparison} compares attack outcomes across all
configurations, including the static scripted results from the companion
blockchain evaluation~\cite{almaayn2024unified} and the MARL empirical
results.  The MARL block rates are generally consistent with the scripted
evaluation for deterministic defenses (both achieve 100\% for self-rating,
admin escalation, and gate bypass) and reveal additional nuance for economic
deterrence mechanisms.

\input{tables/tab_attack_comparison}

\subsection{Convergence Analysis}

Figure~\ref{fig:honest_convergence} shows honest-action percentage over
training episodes.  Convergence to $>99\%$ honest behavior is achieved
within 3{,}000--5{,}000 episodes for single-attack configurations and
within 2{,}000 episodes for the baseline.  The adaptive configuration (C5)
required up to 26{,}158 episodes for its slowest seed.  The multi-vector
configuration (C11) required extended training beyond 10{,}000 episodes and
converged to a bimodal equilibrium: 4/5 seeds to adversarial dominance and
1/5 to honest dominance, by episode 16{,}593.

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_collusion_ci.png}
  \caption{Effect of collusion on confidence interval width.  Colluding
           agents' CI widths grow faster than honest agents', limiting
           score amplification to $\approx\!0.15$ above the honest Nash
           equilibrium.}
  \label{fig:collusion_ci}
\end{figure}

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_sybil_stake.png}
  \caption{Stake dynamics under Sybil attack.  Adversarial agents creating
           Sybil identities deplete their stake faster than honest agents,
           leading to stake-constrained Sybil creation after episode 1{,}500.}
  \label{fig:sybil_stake}
\end{figure}

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_defense_heatmap.pdf}
  \caption{Defense performance heatmap across all 11 configurations.
           Color intensity (YlGn) encodes normalized performance (0--1).
           C1 achieves perfect scores on all metrics; C11 shows degradation
           across honest~\%, defense block rate, and convergence rate due
           to the combined multi-vector adversarial load.}
  \label{fig:defense_heatmap}
\end{figure}

% -----------------------------------------------------------------------
\subsection{Ablation Study}
\label{sec:ablation}
% -----------------------------------------------------------------------

To quantify the contribution of each defense mechanism, we conducted an
ablation study by removing one mechanism at a time from the full C2
configuration and retraining with 3 seeds at up to 5{,}000 episodes.
Table~\ref{tab:ablation} reports the results.

\input{tables/tab_ablation}

With honest behavior alone as the metric, all ablations converge to
$\geq\!99.7\%$ honest action at adversarial bonus $b=0.5$, leaving the
mechanisms indistinguishable by that criterion alone.  The Spearman rank
correlation $\rho$ between final agent scores and true qualities (computed
over 20 deterministic evaluation episodes per seed) provides a sharper
lens on reputation \emph{discrimination} --- the degree to which scores
actually distinguish high-quality from low-quality agents.

\begin{itemize}[noitemsep]
  \item \textbf{No Wilson CI}: $\rho = -0.30 \pm 0.84$ --- the highest
        variance and a near-zero mean reveal bimodal behavior: without
        CI-based weight scaling, scores swing between correlated and
        anti-correlated across seeds, indicating that Wilson CI is critical
        for stable score discrimination.
  \item \textbf{No Temporal Decay}: $\rho = +0.65 \pm 0.34$ --- the best
        discrimination.  Without exponential decay, accumulated positive
        evidence from early honest episodes persists, producing clearer
        score separation.
  \item \textbf{No Stake / No Dispute}: $\rho = +0.57 \pm 0.33$ --- moderate
        discrimination; both mechanisms reduce noise similarly in the
        $b=0.5$ regime.
  \item \textbf{Full system (C2, $b=1.0$)}: $\rho = -0.44 \pm 0.72$ ---
        higher adversarial pressure ($b=1.0$) degrades discrimination
        below the $b=0.5$ ablation level; combined with the bimodal
        convergence, scores are an unreliable signal of true quality.
\end{itemize}

The Wilson CI mechanism is thus doubly important: it supports both honest
convergence (via CI-based penalization of sparse identities) and score
discrimination (via stable weight scaling that prevents bimodal
score-quality alignment).

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_ablation.pdf}
  \caption{Ablation study: honest action percentage and reputation accuracy
           (MAE) for the full system (C2) vs.\ each variant with one defense
           mechanism removed.  The 95\% threshold line (dashed) indicates the
           minimum acceptable honest behavior.}
  \label{fig:ablation}
\end{figure}

% -----------------------------------------------------------------------
\subsection{Parameter Sensitivity}
\label{sec:sensitivity}
% -----------------------------------------------------------------------

Figure~\ref{fig:sensitivity} shows the effect of varying the adversarial
reward bonus $b$ (the primary incentive parameter) on honest action percentage
in tail episodes.  The system maintains $>95\%$ honest behavior for $b \leq 1.0$
(up to $2\times$ the baseline), confirming robustness to moderate incentive
amplification.  At $b = 2.0$ honest behavior begins to degrade for some seeds,
and at $b = 4.0$ convergence fails in the majority of seeds — consistent with
the theoretical expectation that sufficiently strong adversarial incentives
can overcome any fixed penalty structure.

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.75\columnwidth]{figures/fig_parameter_sensitivity.pdf}
  \caption{Incentive sensitivity: honest action percentage (tail) as a function
           of adversarial reward bonus $b$.  The dashed line marks the current
           system default ($b = 0.5$).  The dotted line marks the 95\%
           honest-behavior threshold.}
  \label{fig:sensitivity}
\end{figure}

% -----------------------------------------------------------------------
\subsection{MAPPO vs.\ Independent PPO}
\label{sec:mappo_vs_ippo}
% -----------------------------------------------------------------------

We compare MAPPO (shared policy) against Independent PPO (per-agent policy,
no parameter sharing) on C2 and C11.  Figure~\ref{fig:mappo_vs_ippo} shows
the training curves.  MAPPO consistently reaches the 95\% honest-behavior
threshold faster than IPPO on both configurations, reflecting the benefit of
shared policy learning: honest agents can collectively learn the optimal
deterrence response more efficiently when they share gradient updates.  On C11,
the performance gap is larger due to the coordinated multi-vector nature of
the adversarial population, which MAPPO's joint observation processing handles
more robustly.

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_mappo_vs_ippo.pdf}
  \caption{MAPPO vs.\ Independent PPO: honest-action percentage over training
           (smoothed, mean $\pm$ std across seeds) for C2 (mixed adversaries,
           left) and C11 (all attacks, right).  MAPPO converges faster and to
           a higher equilibrium on both configurations.}
  \label{fig:mappo_vs_ippo}
\end{figure}

% -----------------------------------------------------------------------
\subsection{C11 Action Decomposition}
\label{sec:c11_decomp}
% -----------------------------------------------------------------------

Figure~\ref{fig:c11_decomp} shows the action distribution in the tail
episodes for the best-performing C11 seed.  Adversarial agents in C11
learn a mixed strategy dominated by honest rating actions ($\approx\!99\%$
of all actions), with a small residual of dishonest ratings and hard attacks.
The tail attack-attempt rate of $\approx\!0.7\%$ reflects the learned
unprofitability of hard attacks: provenance replay (A11) and admin escalation
(A8) are most frequently attempted but always blocked.  The dominance of
honest behavior in the best seed illustrates that even under the most
adversarial setting, a well-trained MAPPO policy can discover the honest Nash
equilibrium.

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/fig_c11_action_decomposition.pdf}
  \caption{C11 action distribution in tail episodes (best seed, left) and
           blocked attacks by type (right).  Provenance replay (A11) and
           admin escalation (A8) dominate the blocked-attack counts, reflecting
           the frequency with which adversarial agents attempt these
           high-penalty actions before learning they are unprofitable.}
  \label{fig:c11_decomp}
\end{figure}

% -----------------------------------------------------------------------
\subsection{Terminal Reward Ablation}
\label{sec:terminal}
% -----------------------------------------------------------------------

We evaluate a reward shaping variant that adds a terminal reputation-progress
component at episode end:
\begin{equation}
  R_{\text{terminal}} = 0.5 \times (\mu_i^{\text{final}} - \mu_i^{\text{initial}}),
  \label{eq:rterminal}
\end{equation}
where $\mu_i^{\text{initial}} = \alpha_0/(\alpha_0+\beta_0) = 0.5$ (from the
Beta prior) for all agents.  This connects the RL objective directly to
actual reputation outcomes --- agents that end the episode with higher scores
than they started receive an additional reward proportional to the improvement,
incentivising score-building behavior over step-by-step rating tactics.

We retrain C2 (mixed adversaries) and C11 (comprehensive) with this
modification using 3 seeds each (\texttt{config2\_terminal},
\texttt{config11\_terminal}).  Results are reported in
Table~\ref{tab:terminal}.

\begin{table}[h]
\centering
\caption{Terminal reward ablation: C2 and C11 with $R_{\text{terminal}}=0.5\times(\mu^{\text{final}}-0.5)$ vs.\ baseline.}
\label{tab:terminal}
\footnotesize
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}L{3.2cm}C{1.2cm}C{1.6cm}C{1.6cm}@{}}
\toprule
\textbf{Configuration} & \textbf{Conv.} & \textbf{Honest \%} & \textbf{Eval Reward} \\
\midrule
C2 baseline           & 4/5 & $73.6\pm38.8$\% & $-7.06\pm9.37$ \\
C2 + terminal reward  & \textit{pending} & \textit{pending} & \textit{pending} \\
C11 baseline          & 5/5 & $20.0\pm40.0$\% & $-2.62\pm2.17$ \\
C11 + terminal reward & \textit{pending} & \textit{pending} & \textit{pending} \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\subsection{Statistical Significance}
\label{sec:stats}
% -----------------------------------------------------------------------

Table~\ref{tab:statistical} reports one-sample $t$-tests (one-tailed,
threshold $= 95\%$) for honest-behavior percentage across seeds.
Seven of the 11 configurations show statistically significant honest behavior
above the 95\% threshold ($p < 0.05$).  The four exceptions are C2 (mixed),
C3 (Sybil flooding), C6 (self-rating), and C11 (comprehensive), all of which
exhibit high cross-seed variance arising from bimodal equilibria.  For C11,
the fully converged result ($20.0\% \pm 44.7\%$) reflects 4/5 seeds settling
in the adversarial-dominant basin; the $t$-statistic of $-3.75$ is strongly
negative, confirming that the aggregate honest behavior is significantly
\emph{below} the 95\% threshold.  For C2, one seed converged to an
adversarial-dominant equilibrium after extended training (honest \% = $0.0\%$),
reducing the aggregate from $99.7\%$ to $73.6\%$ and rendering the
difference from threshold non-significant ($t = -1.10$, $p = 0.834$).
C5 and C10 improved with extended training, both reaching $100.0\%$ honest
behavior ($t = \infty$, $p < 0.001$).

\input{tables/tab_statistical}
