# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.env

# PyTorch checkpoints — large binary files, not committed
results/checkpoints/

# Per-seed training logs — large JSON files (10k episodes each)
results/training_logs/*_seed*_log.json
results/training_logs/*_seed*.json
results/training_logs_v1_backup/

# Ablation / sensitivity / IPPO per-seed logs (same exclusion rule)
results/ablation/*_seed*_log.json
results/ablation/*_seed*.json
results/ablation/checkpoints/
results/sensitivity/*_seed*_log.json
results/sensitivity/*_seed*.json
results/sensitivity/checkpoints/
results/ippo/*_seed*_log.json
results/ippo/*_seed*.json
results/ippo/checkpoints/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temp / log files
/tmp/
*.log
*.tmp

# ---------------------------------------------------------------
# Tier 1B sweep results — results/tier1b/sweep/ alone is ~19 GB
# (540 shards, 20,629 checkpoints). Raw results never enter git.
# Already-tracked small artifacts under results/ are unaffected;
# this rule only stops NEW results from being added.
# ---------------------------------------------------------------
results/
